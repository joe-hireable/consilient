Paused 2026-08-25 ~16:15 by the orchestrator, deliberately and reversibly.

WHY: a verified fix for the largest single failure in the system -- 3,895 of 4,930 recorded
dispatch deaths, 79%, from the trajectory lock sitting on byte 0 where every reader reads --
is written, tested and cannot land, because landing it needs a green suite and the suite
cannot complete honestly under ~50 concurrent agent pytest runs. The gate that decides
whether work may land had itself become a casualty of the saturation.

Stopping the loop does not kill anything in flight; those drain on their own deadlines. It
only stops NEW dispatches being added to a machine that is not completing the ones it has.

TO RESUME: delete this file. `.harness/build_loop.py` checks for it at the top of every tick
and exits cleanly; the scheduled task ConsilientBuildLoop restarts it.
