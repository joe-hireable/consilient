# [Project Name] Design System

> Replace `[Project Name]` with the product or surface name. This file is the
> design contract: once tokens are locked, agents treat them as non-negotiable.
> Adapted from the Open Design 9-section format
> (https://github.com/nexu-io/open-design, Apache-2.0).

## 1. Visual Theme & Atmosphere

<!-- Describe the mood, density, and motion intensity. Name 1–3 reference
     products or editorial traditions ("think Monocle meets a terminal").
     State what the design is NOT as clearly as what it is. -->

<!-- American spelling in this heading for interop with Open Design's validator,
     which string-matches section names. British English in body prose per AGENTS.md. -->
## 2. Color Palette & Roles

<!-- List every colour as a named role + hex value. At minimum:
     Background, Surface, Foreground (text), Accent (primary),
     Accent (secondary/muted), and any semantic colours (error, success).
     State "never use pure black / pure white" or similar constraints. -->

## 3. Typography Rules

<!-- Font stacks for Display, Body, and Mono. Scale (px or rem).
     Line-height per role. Letter-spacing rules at display sizes.
     State how many typeface families are permitted. -->

## 4. Component Stylings

<!-- Buttons, cards, inputs, links — the recurring elements. Give border-radius,
     padding, shadow rules, and state transitions (hover, focus, active).
     Reference colour roles, not raw hex values. -->

## 5. Layout Principles

<!-- Grid system, max-width, gutter size. Hero section height rules.
     Section spacing at each breakpoint. One-accent-per-screen rule
     or equivalent constraint. -->

## 6. Depth & Elevation

<!-- How many elevation levels exist and what uses each. Shadow
     specification per level. State which elements may NOT have shadows. -->

## 7. Do's and Don'ts

<!-- 3–5 Do's and 3–5 Don'ts. Be concrete: "no gradients", "serif for
     numbers when they matter", "no border-radius above 24 px". -->

<!-- American spelling for interop; see § 2 comment. -->
## 8. Responsive Behavior

<!-- Breakpoints (px values). Column count per breakpoint. What stacks,
     what hides, what rescales. Padding reduction rules. -->

## 9. Agent Prompt Guide

<!-- Instructions for a coding agent generating artefacts under this system.
     State the non-negotiable token rule: do not invent hex values outside
     the palette. State the maximum accent-element count per viewport.
     State when this system is NOT the right answer. -->
